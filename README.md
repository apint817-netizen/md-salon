# MD Beauty Salon Website

Премиальный сайт для салона красоты "МД".

## 🚀 Как запустить проект

1.  **Установка зависимостей**:
    ```bash
    npm install
    ```

2.  **Запуск в режиме разработки**:
    ```bash
    npm run dev
    ```
    Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 🛠 Технологии

-   **Next.js 14+** (App Router)
-   **Tailwind CSS** (Стилизация)
-   **Framer Motion** (Анимации)
-   **Lucide React** (Иконки)

## 📁 Структура проекта

-   `src/components/ui` - Базовые UI компоненты (Button, Container, Section)
-   `src/components/layout` - Компоненты макета (Header, Footer)
-   `src/components/sections` - Секции главной страницы (Hero, Services, Portfolio, etc.)
-   `src/app/globals.css` - Глобальные стили и переменные темы

## 🎨 Настройка

-   **Цвета**: Измените переменные `--color-gold-500` и `--color-gold-600` в `src/app/globals.css`.
-   **Контент**: Тексты и данные находятся прямо в компонентах секций (например, `src/components/sections/Services.tsx`).

## 📦 Деплой

Проект готов к деплою на Vercel.
Просто импортируйте репозиторий в Vercel dashboard, и сайт будет развернут автоматически.
